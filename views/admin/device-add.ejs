<!DOCTYPE html>
<html>
<head>
    <% include partial/head.ejs %>
</head>
<body class="admin-page">

<% include partial/menu.ejs %>

<div class="container-fluid">
    <div class="row content">
        <div class="main-content col-sm-9 col-sm-offset-3">
            <h1 class="page-header">Добавление устройства</h1>
            <div class="row">
                <div class="col-sm-12">
                    <form role="form" method="post">
                        <div class="form-group">
                            <label for="uniqId">Введите уникальный ID устройства </label>
                            <% if (message) { %>
                            <label for="uniqId" class="text-danger"><%= message %></label>
                            <% } %>
                            <input name="uniqId" id="uniqId" class="form-control" value="<%= prev ? prev.uniqId : '' %>">
                            <p class="help-block">
                                (<strong>IMEI</strong> для телефона, <strong>MAC</strong> адрес Wi-Fi или Bluetooth модуля,
                                <strong>Serial</strong> номер, <strong>ID</strong> внутреннего журнала устройств <br>
                                или оставьте поле пустым для автоматиеческой генерации ID, <br>
                                Рекомендуется указать вначале тип выбранного id.
                                например <strong><cite>Serial F2MKG1B1DTWE</cite></strong>)
                            </p>
                        </div>
                        <div class="form-group">
                            <label for="deviceType">Тип устройства*</label>
                            <input name="deviceType" id="deviceType" required class="form-control" value="<%= prev ? prev.deviceType : '' %>"
                                   placeholder="Смартфон, планшет, кабель, зарядка ..." list="deviceType">
                            <datalist id="deviceType">
                                <option value="Смартфон"></option>
                                <option value="Фаблет"></option>
                                <option value="Планшет"></option>
                                <option value="Смартчасы"></option>
                                <option value="Кабель"></option>
                                <option value="Зарядка"></option>
                            </datalist>
                            <p class="help-block">*Обязательное поле</p>
                        </div>
                        <div class="form-group">
                            <label for="model">Модель</label>
                            <input name="model" id="model" class="form-control" value="<%= prev ? prev.model : '' %>">
                        </div>
                        <div class="form-group">
                            <label for="name">Описание</label>
                            <input name="name" id="name" class="form-control" value="<%= prev ? prev.name : '' %>">
                        </div>
                        <button type="submit" class="btn btn-default">Сохранить</button>
                        <button type="reset" class="btn btn-default">Сбросить</button>
                    </form>
                    <br>
                </div>
            </div>
        </div>
    </div>
</div>
</body>
</html>
